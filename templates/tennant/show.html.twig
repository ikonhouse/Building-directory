{% extends 'base.html.twig' %}

{% block title %}Tennant{% endblock %}

{% block body %}
    <div class="container">

    <h1>Tennant</h1>
    <br>
    <a href="{{ path('tennant_index') }}">back to list</a>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tennant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tennant.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ tennant.number }}</td>
            </tr>
        </tbody>
    </table>



    <a href="{{ path('tennant_edit', {'id': tennant.id}) }}">edit</a>

    {{ include('tennant/_delete_form.html.twig') }}
    </div>

{% endblock %}
